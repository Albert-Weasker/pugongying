# 蒲公英 (Pugongying)

一个极简的匿名书写空间，让话语像蒲公英一样被风带走。

## 特性

- 🌱 **匿名书写**：无需注册，无需回应
- 💨 **随风而去**：写下的话会被风带走，不保存来源
- 🌍 **世界状态**：感受世界的存在，但不形成关系
- 🎵 **沉浸体验**：视频背景与背景音乐

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **数据库**: PostgreSQL (Neon)
- **ORM**: Prisma
- **部署**: Vercel (推荐)

## 快速开始

### 1. 安装依赖

```bash
pnpm install
# 或
npm install
```

### 2. 配置环境变量

创建 `.env.local` 文件：

```bash
DATABASE_URL=your_postgresql_connection_string
```

### 3. 初始化数据库

```bash
# 生成 Prisma Client
npm run db:generate

# 创建数据库表（选择其中一种方法）
npm run db:push
# 或直接在 Neon 控制台执行 prisma/migrations/init.sql
```

### 4. 导入初始数据（可选）

```bash
npm run import:messages
```

### 5. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000)

## 项目结构

```
pugongying/
├── app/                    # Next.js App Router
│   ├── components/         # React 组件
│   ├── api/               # API 路由
│   └── page.tsx           # 首页
├── lib/                   # 工具函数
│   ├── db/               # 数据库相关
│   └── i18n.ts           # 国际化
├── prisma/               # Prisma 配置
│   └── migrations/       # 数据库迁移
└── scripts/              # 工具脚本
```

## 核心概念

### 风池 (Wind Pool)

- 每一句话只存在一次
- 被看见，就是它的结束
- 不是对这个人消失，是对整个世界消失

### 匿名会话

- 不需要用户表
- 只需要知道"当前是谁"
- 通过 session 跟踪已看过的消息

## 开发命令

```bash
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run start        # 启动生产服务器
npm run db:generate  # 生成 Prisma Client
npm run db:push      # 推送 schema 到数据库
npm run db:studio    # 打开 Prisma Studio
npm run import:messages  # 导入初始消息
```

## 部署

推荐使用 [Vercel](https://vercel.com) 部署：

1. 将代码推送到 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量 `DATABASE_URL`
4. 部署完成

## License

MIT
